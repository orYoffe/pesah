{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "users": {
      "$userid": {
        ".read": "auth.uid == $userid",
        ".write": "auth.uid == $userid"
      }
    }
  }
}
